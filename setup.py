from setuptools import setup, find_packages

setup(
    name="msc4155-synapse-invite-filter",
    version="0.0.1",
    packages=find_packages(),
    description="MSC4155 Synapse Invite Filter",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
